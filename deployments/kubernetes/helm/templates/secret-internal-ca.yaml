{{- if eq .Values.ingress.letsEncrypt.enabled false }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ingress.CAname }}
type: Opaque
stringData:
  cacerts.pem: |
        {{ .Values.ingress.CAcert }}
{{- end }}